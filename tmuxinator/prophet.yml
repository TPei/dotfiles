name: prophet
root: ~/Code/gapfish_prophet
windows:
  - server:
    layout: even-horizontal
    panes:
      - docker-compose up -d
      - exit # just to have 2/1
      - status
  - editor:
    layout: even-horizontal
    panes:
      - vim
